import { Button } from "./Button";
import { Inputs } from "./Inputs";

export const Login = () : any => {
    return(
        <form>
            <Inputs />
            <Button text="Submit"></Button>
        </form>
    )
}